1，赋值
	python中的赋值都是对象内存地址的引用
		>>> L = [1,2,3,[1,2,3]]
		>>> L1 = L
		>>> L[3].append(32)
		>>> L
		[1, 2, 3, [1, 2, 3, 32]]
		>>> L1
		[1, 2, 3, [1, 2, 3, 32]]
		>>> id(1)
		5373152
		>>> id(L)
		29003904
		>>> id(L1)
		29003904
	上例中L1与L是一样的，表示的是一个对象，只是表示的符号不一样
2，copy（浅拷贝）
	先创建一个新对象，然后将原对象里的所有元素的内存地址指向新对象的地址，所以修改原对象里面的元素时，若此元素不可变，将重新赋值，此元素的内存地址改变，新对象里对应的此元素的内存地址不变，故对新对象无影响；若此元素可变，修改它，内存地址不变，这是新对象的对应的元素也跟着变。
3，deepcopy
	新瓶装新酒
浅拷贝是循环赋值
深拷贝是递归赋值
